IRISNativeReverseProxy

This is an example of using the IRIS Native interface from .NET

In this example, an Reverse proxy object gets created to manipulate IRIS objects instances from .NET. The example uses this to copy a local file to the %Stream.BinaryFile on the server.